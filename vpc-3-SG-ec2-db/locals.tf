locals {
    anywhere   = "0.0.0.0/0"
    ssh_port   = 22
    http_port  = 80
    https_port = 443
    pg_port    = 5432
    tcp        = "TCP"
}

# Note:- For brevity, local values are often referred to as just "locals" as the meaning is clear from context.